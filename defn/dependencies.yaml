# Project build dependencies for compilation
# List all C source files (relative to project root) that should be compiled/linked

sources:
  - src/matrix.c
  
include_dirs:
  - src/
  - lib/simplepim/